"""{{ pascal }} — business logic.

Services hold the operations that are not simply "read a row": the rules,
the orchestration, the things worth testing on their own. Keeping them out of
handlers means the same logic serves an HTTP route, a queued job and a
scheduled task without being rewritten.
"""

from __future__ import annotations


class {{ pascal }}:
    """{{ human }}."""

    async def __call__(self) -> None:
        """Run the operation this service performs."""
        raise NotImplementedError("Implement {{ pascal }}.__call__")
