"""The {{ pascal }} event.

Events describe something that happened, in the past tense. They carry the
facts a listener needs and nothing more.
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class {{ pascal }}:
    """{{ human }}."""

    # Carry identifiers rather than model instances: an event may be handled
    # in another process, where the instance would be meaningless.
    pass
