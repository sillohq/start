"""The {{ pascal }} seeder.

Seeders fill the database with data you want present in development — a few
users, some reference rows. Keep them idempotent so running twice is harmless.
"""

from __future__ import annotations


async def run() -> None:
    """Insert this seeder's records."""
    raise NotImplementedError("Implement the {{ snake }} seeder")
