"""The {{ pascal }} scheduled task."""

from __future__ import annotations

from sillo.work.scheduler import CronTrigger


async def {{ snake }}() -> None:
    """{{ human }}."""
    raise NotImplementedError("Implement {{ snake }}")


def register(scheduler) -> None:
    """Schedule this task.

    Called from `app/tasks/__init__.py`. The cron expression is
    "minute hour day month weekday" — this one runs hourly.
    """
    scheduler.schedule(
        {{ snake }},
        trigger=CronTrigger("0 * * * *"),
        name="{{ snake }}",
    )
